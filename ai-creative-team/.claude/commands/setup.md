---
description: Interactive onboarding — brand brief, keys, MCP ping, folder scaffold
---

Follow `setup.md` in the repository root, step by step. Ask the user
one question at a time, never echo API keys back, and finish by running
the MCP `--ping` health checks.
